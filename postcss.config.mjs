// The design system is hand-authored plain CSS in app/globals.css (pixel-specific,
// preserved verbatim from the design export). No Tailwind. PostCSS runs no plugins.
/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {},
};

export default config;
